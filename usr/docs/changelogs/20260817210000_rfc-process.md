## Participants

- Andrei Makarov

## Decisions

rfc-process is a dual export like claims-audit and dependency-policy. The fragment states when to write an RFC and points at the skill. The skill holds id claims, the shared template, types, statuses, isolation default, and how to flatten an existing tree without renumbering.

Project bands, extra product tests, and isolation-on stay in each repository rfcs/README.md. The package names only the rfcs/ layout.

When 0001 is already a product document, the process RFC takes the next free id in the process band.

## Effects

Added packages/rfc-process 1.0.0. Wired the skill into this repo Prayfile tree. Updated README consumer example and catalog.

pray plan, pray apply, and pray verify in this repo exited 0. apply warned that AGENTS.md had local edits outside managed spans and would be overwritten. The lockfile also recorded writing-prose 3.1.0 and community-contributing/governance 1.1.0 from path sources already in the working tree.

## Next

Consumers switch from a local path to git ~> 1.0 after tag v1.3.0. This release also ships writing-prose 3.1.0 and the two community-doc 1.1.0 bumps.

## Source

Follows polyrun.rb and kiskolabs/pray RFC 0001 for types, statuses, and template sections.

