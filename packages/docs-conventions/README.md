# amkisko/docs-conventions

Human docs live under `docs/`. Durable agent and engineering trace lives under `usr/docs/` (`issues`, `plan`, `changelogs`, `meetings`, `dependencies`, `tasks`, `ideas`). Trace **Participants** list humans only. Never leak absolute local paths or private data into either tree.
