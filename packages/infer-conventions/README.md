# amkisko/infer-conventions

Learn how a repository actually writes, then encode genuine forks as checks that fail on violation. The skill documents reality. It does not propose a better pattern, and it does not write agent convention markdown.

Exports:

- `infer-conventions` skill — orient, sweep genuine forks, map architecture, confirm, add a formatter, linter, architecture-test, or CI check

Tree this skill under `.agents/skills`. Do not compose it into AGENTS.md.

Related: `amkisko/working-rules`, `amkisko/minimal-implementation`, `amkisko/dependency-policy`, and `amkisko/engineering-audit`.
