# Prayers 1.23.0 catalog publish

## Participants

Andrei Makarov.

## Decisions

Cut CHANGELOG.md 1.23.0. Include the catalog topic rewrite and make catalog-topics already on main. Do not bump package versions. Topics are an annotation layer. Artifact hash, tree hash, and signature stay.

pray 1.20.0 still ranks function words into derived_metadata.topics. The workaround is usr/scripts/catalog_topics.rb after pray publish. Evidence: docs/dependencies/20260922124859_pray-derived-topics-function-words.md.

Tag v1.22.0 at the 1.22.0 merge commit. That heading shipped without a git tag or GitHub release.

This pass is catalog annotation and publisher tooling. Person-facing surface: CHANGELOG.md and the README publish section. Hierarchy is version heading then imperative bullets, newest first. Copy names the topic rewrite, the make target, and that versions stay. README tells operators that make publish rewrites topics. No interactive, empty, loading, or error states. A catalog reader can search by summary tokens instead of function words.

## Effects

Catalog topic rewrite and locator retarget already landed on main before this heading. This pass cut the heading and documented the publisher command. AGENTS.md is 19,782 bytes, 3,398 over the 16 KiB house line. Prayfile.lock and AGENTS.md stayed unchanged. Did not rerun make publish.

Validation commands and observed results:

- make test: first sandbox run failed two check_artifacts cases with git init Operation not permitted in tmpdir; rerun with full permissions: exit 0; validate_skill 13 runs, 43 assertions; catalog_topics 8 runs, 33 assertions; check_artifacts 9 runs, 37 assertions; 0 failures
- make validate-skills: exit 0; 13 skills valid
- make catalog-topics: exit 0; catalog-topics: ok
- make plan then make apply: exit 0; 36 packages, 1 local file, everything up to date; AGENTS.md 19,782 bytes
- make verify && make drift && make check-artifacts: exit 0; check-artifacts reported ok
- git diff --check: exit 0, no diagnostics
- pray version: pray 1.20.0

## Next

Tag v1.22.0 at c695991 and v1.23.0 after merge to main. Consumers need no pin bump. Leave pray STOPWORDS as an upstream defect.

## Source

docs/dependencies/20260922124859_pray-derived-topics-function-words.md
Downstream: CHANGELOG.md 1.23.0
