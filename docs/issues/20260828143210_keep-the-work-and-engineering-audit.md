# Keep the work and engineering audit

Live work for this unpublished pass. Skill and fragment text is inference input: resource, trace, and boundary modes skipped because these files never become executed bytes and there is no plant.

## Participants

- amkisko

## Decisions

keep-the-work: stay on the place they used with a matching refusal; redisplay failed forms with passing and failing answers; unknown identifiers as real refusals; stay on acknowledged forbidden; warn before timeout and restore after re-auth. Rewrite location only when the identifier itself changed. Do not grow the living fragment until a product needs the next bullets. Publisher omits always-on keep-the-work so AGENTS.md stays under 16 KiB.

engineering-audit 2.5.0 resource and budget: run for every tree that can execute; skip only when the tree never becomes executed bytes. Utilization is memory, CPU, storage, network, energy or a stated proxy. Capacity is the named ceiling. Ask whether this tree starves other software on shared hardware.

engineering-audit 2.5.0 trace and identification: same run-or-skip gate. RFC 6973 section 7.1 is the catch list, including retention of identifiers and IP addresses. TLS handshake, SNI, and certificate exchange are the channel.

security 1.1.0: distinguish redacted secret hash from device fingerprint. Treat identifiers, IP addresses, device marks, and combined attributes as personal data. Silent analytics ids, advertising ids, canvas or hardware probes, and marks that survive logout are security events. Do not paste GDPR, ATT, or Play recipes as house law.

docs-conventions 2.2.0: usr/docs has four timestamp trees. issues is live work (contract, findings, open next). Pitch, plan, and the open queue stay in that file. changelogs is what shipped. meetings is one sitting: who was there and what they agreed. dependencies is upstream defect evidence from real work, with its own headings. Templates live in the docs-conventions README. dependency-issues 2.1.0 stores under dependencies. writing-prose 3.3.0 applies plain prose to all usr/docs traces.

Gardener and polyrun wait for their own issue notes. Do not invent those packages here.

## Effects

Encoded as keep-the-work 1.0.0 and 1.1.0, preferred-stack 1.1.0 through 1.3.0, minimal-implementation 1.1.0, engineering-audit 2.2.0 through 2.5.0, security 1.1.0.

2.4.0 findings encoded in 2.5.0. Second-round mediums patched in the same 2.5.0 rebuild. Third round closed at two lows. security 1.1.0 tracking cautions encoded.

Collapsed this episode from five usr/docs copies into one issues file plus one changelog. Restored meetings and dependencies as named trees with purpose and templates. Product changelog stays 1.7.0; 1.8.0 through 1.12.0 were never committed.

Later pass: prose and consistency audit of that restore. Source now cites docs-conventions 2.2.0. The issues parenthetical says pitch, plan, and queue stay in that file. CHANGELOG 1.7.0 and the README state the four trees without a drop-clause. Em dashes in the fragments this pass touched became colons or full stops. make apply: AGENTS.md 16328 bytes (56 under 16 KiB). Rebuilt docs-conventions 2.2.0 at 2078 bytes, writing-prose 3.3.0 at 1269 bytes, dependency-issues 2.1.0 at 1335 bytes. docs-conventions fragment 1667 bytes. make verify exit 0. make drift exit 0. make check-artifacts ok after git add of those three praypkg files.

## Next

Consumers bump docs-conventions ~> 2.2, writing-prose ~> 3.3, dependency-issues ~> 2.1, dependency-policy ~> 3.1, keep-the-work ~> 1.1 after preferred-stack, preferred-stack ~> 1.3, engineering-audit ~> 2.5, security ~> 1.1, then pray install.

Commit unpublished work when ready. Write gardener and polyrun issue notes before inventing packages. Do not grow keep-the-work until a product needs the next bullet. Add the engineering-audit README skip on the next unpublished rebuild if one happens.

## Source

Upstream: packages/keep-the-work, packages/engineering-audit 2.4.0 and 2.5.0, packages/security 1.1.0, RFC 6973 section 7.1, packages/docs-conventions 2.2.0
Downstream: usr/docs/changelogs/20260828143210_keep-the-work-and-engineering-audit.md, CHANGELOG.md 1.7.0
